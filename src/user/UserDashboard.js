import React, { PropTypes } from 'react';
import Base from "../core/Base.js"

const UserDashboard = ({ className }) => {
    return (

    	<Base title="User Dashboard">
    	<h1 className="text">Welcome to User Dashboard</h1>
    	</Base>

    	);

};


export default UserDashboard;
